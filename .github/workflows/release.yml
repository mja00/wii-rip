name: release

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - v*
  workflow_dispatch:

permissions:
  contents: write

env:
  PACKAGE_NAME: wii-rip-linux-x86_64
  DOLPHIN_REF: 2603a
  DOLPHIN_TOOL_PATH: build/dolphin-tool-cache/dolphin-tool

jobs:
  ubuntu-release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Restore Cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Set dolphin-tool cache period
        run: echo "DOLPHIN_CACHE_PERIOD=$(date -u +%Y-%m)" >> "$GITHUB_ENV"

      - name: Restore dolphin-tool cache
        uses: actions/cache@v4
        with:
          path: ${{ env.DOLPHIN_TOOL_PATH }}
          key: dolphin-tool-${{ runner.os }}-${{ env.DOLPHIN_REF }}-${{ env.DOLPHIN_CACHE_PERIOD }}

      - name: Install Ubuntu build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            gettext \
            ninja-build \
            pkg-config \
            wit

      - name: Build dolphin-tool helper
        run: |
          ./packaging/build_dolphin_tool.sh \
            --ref "$DOLPHIN_REF" \
            --output "$DOLPHIN_TOOL_PATH" \
            --skip-if-present

      - name: Run linters
        run: cargo clippy --all-targets -- -D warnings

      - name: Run tests
        run: cargo test

      - name: Package bundled release
        run: |
          ./packaging/build_release.sh \
            --name "$PACKAGE_NAME" \
            --tar \
            --dolphin-tool "$DOLPHIN_TOOL_PATH" \
            --wit "$(command -v wit)"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.PACKAGE_NAME }}
          path: dist/${{ env.PACKAGE_NAME }}.tar.gz
          if-no-files-found: error

      - name: Publish GitHub release assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/${{ env.PACKAGE_NAME }}.tar.gz
